# The deployed RocketRide Compare endpoint. Leave blank for the synthetic UI demo.
VITE_ROCKETRIDE_PIPELINE_URL=

# Never put Linkup, RocketRide, OpenAI, or model-hosting secrets in VITE_* variables.
# Configure those server-side in RocketRide/your model host instead.
