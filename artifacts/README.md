# Experiment artifacts

Check in small, reproducible metrics JSON, confusion matrices, ablation tables, and plots only after real runs. Store large model weights elsewhere and record immutable references and checksums here.

