# Frontend lane

Own `apps/web/**`. Read the root and local `AGENTS.md` plus `docs/demo-script.md`. Work from the stable demo response, preserve visible demo labeling, render classifications without re-deriving them, and finish with `npm run lint && npm run test && npm run build` from `apps/web`.

