# Frontend lane

Own `apps/web/**`. Read the root and local `AGENTS.md` plus `docs/demo-script.md`. The supplied self-contained `index.html` is the only frontend; do not create a parallel React app. Preserve visible demo labeling, render classifications without re-deriving them, and finish with `npm run lint && npm run test && npm run build` from `apps/web`.
